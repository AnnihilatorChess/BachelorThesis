# Vendored from camlab-ethz/ConvolutionalNeuralOperator (CNO2d_classic/torch_utils),
# itself vendored from NVlabs/stylegan3. Original repo has no __init__.py here and
# relies on implicit namespace packages; we add this explicit marker so the absolute
# `import torch_utils` alias set up in ../__init__.py resolves cleanly.
